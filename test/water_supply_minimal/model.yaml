sector_models:
  - water_supply
timesteps:
  timesteps.yaml # Set of timesteps
assets: []
planning:
  pre_specified:
    use: true
    files:
      - pre-specified.yaml # The build instructions
  rule_based:
    use: false
  optimisation:
    use: false