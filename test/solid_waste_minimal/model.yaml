sector_models:
  - name: solid_waste
    path: /vagrant/models/solid_waste/run.py
    classname: SolidWasteModel
    config_dir: "."
timesteps: timesteps.yaml
assets:
  - assets_initial.yaml # Initial conditions for the model (pre-existing capacity)
asset_types:
  - assets.yaml # Attributes for each asset in the set of assets
timesteps: timesteps.yaml
planning:
  pre_specified:
    use: true
    files:
      - pre-specified.yaml # The build instructions
  rule_based:
    use: false
    files: []
  optimisation:
    use: false
    files: []
